{
  "name": "dereck-mongodb",
  "version": "0.0.2",
  "repository": "git@github.com:dereckmezquita/dereck-mongodb.git",
  "author": "dereckmezquita <dereck@mezquita.io>",
  "private": true,
  "scripts": {
    "format": "prettier --write .",
    "envs": "export MONGO_INITDB_ROOT_USERNAME=dereck; export MONGO_INITDB_ROOT_PASSWORD=yeet; export DOCKER_EXPOSE_PORT=3000;",
    "docker-run:dev": "yarn run envs docker-compose -f docker-compose.yml up -d",
    "docker-stop:dev": "yarn run envs docker-compose -f docker-compose.yml down",
    "docker-data-delete:dev": "docker volume rm <dereck-mongo-data | dereck-mongodb_mongo-data | docker-compose-mongo_mongo-data>"
  },
  "prettier": {
    "semi": true,
    "trailingComma": "none",
    "singleQuote": true,
    "printWidth": 80,
    "tabWidth": 2,
    "useTabs": false
  },
  "devDependencies": {
    "husky": "^9.0.11",
    "prettier": "^3.2.5"
  }
}
